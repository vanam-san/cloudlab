# Secrets - never commit to GitHub
.env
.env.*
!.env.example

# Generated by setup.sh (plane.env holds SECRET_KEY, DB passwords, ...).
# Re-created by ./run.sh on a fresh clone, so nothing here is needed in git.
plane-app/
*.log